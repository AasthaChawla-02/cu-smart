# CU Connect — Chatbot (Frontend + Backend)


## Overview
This project provides a lightweight chatbot for CUIMS. The backend first checks a local knowledge base (`data.json`) and if no good match is found it forwards the query to a Gemini-style API.


## Quick start (local)
1. Copy files into a directory with this structure: